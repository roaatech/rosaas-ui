import styled from 'styled-components'
export const Wrapper = styled.div`
  .label {
    margin-right: 0.25rem;
    background: '#eff9f6';
  }
  .green {
    color: var(--green);
    background: '#eff9f6';
  }
  .red {
    color: var(--red);
    background: '#eff9f6';
  }
`
